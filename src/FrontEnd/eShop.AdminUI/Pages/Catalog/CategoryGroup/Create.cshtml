@page
@model eShop.AdminUI.Pages.Catalog.CategoryGroup.CreateModel
@{
}

<div class="card shadow">
    <div class="card-body">
        <div class="">
            <h4 class="card-title">Create category group</h4>
        </div>

        <form method="post">
            <div class="mb-3">
                <label class="form-label" asp-for="CreateCategoryGroupViewModel.Name">Name</label>
                <input class="form-control" asp-for="CreateCategoryGroupViewModel.Name">
            </div>
            <div class="form-group">
                <a asp-page="/Catalog/Category/Index" class="btn btn-primary">Return to list</a>
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
